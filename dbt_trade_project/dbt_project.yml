name: trade_pipeline
version: 1.0.0
config-version: 2
profile: trade_pipeline
model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
clean-targets: ["target", "dbt_packages"]
models:
  trade_pipeline:
    staging:
      +schema: STAGING
      +materialized: view
    intermediate:
      +schema: INTERMEDIATE
      +materialized: view
    marts:
      +schema: CURATED
